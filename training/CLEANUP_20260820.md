# Local model promotion and cleanup — 2026-08-20

This cleanup was performed locally on branch `teammate/chi-lou-local`. Nothing
was committed or pushed.

## Active item classifier

- Active candidate: `candidate_v23_full40_seed_frozen_sparse_search`
- Taxonomy: 40 item classes
- Browser model: `public/models/waste_classifier.onnx`
- Training checkpoint: `training/checkpoints/candidate_v23_full40_seed_frozen_sparse_search.pt`
- Candidate metadata: `training/checkpoints/candidate_v23_full40_seed_frozen_sparse_search.json`
- Evaluation dataset retained for reproducibility:
  `training/candidate_datasets/candidate_v22_full40_balanced_with_sparse_organic_feedback`

v23 remains below the intended 70% top-1 target and is active only because it
was explicitly selected for further local field testing. See
`public/models/MODEL_CARD.md` for its measured results and limitations.

## Original model rollback

The complete previous 36-class active model is preserved at:

`training/model_archive/original-active-36class-20260820/`

The archive contains the PyTorch and ONNX models, labels, class list, original
model card and evaluation artifacts. Its ONNX SHA-256 is
`510fc911c752b67c01b541cc0968cebc62aa0d07bc68661c66d10485c3fc6dd6`.

## Dataset review

- Removed two fabric pouches/sleeves that had been mislabeled and heavily
  oversampled as `phone_case`.
- Rebuilt `phone_case/train` from three reviewed rigid phone-case photos.
- Replaced the only `hair_clip` validation image, an out-of-distribution antique
  artifact, with a reviewed single-item bow hair clip.
- Removed five non-image/cache files from the classifier dataset.
- Removed empty dataset folders.
- Revalidated all 40 class folders and all readable images.

Audit records:

- `training/source_manifests/cleanup-rejections-20260820.json`
- `training/source_manifests/cleanup-incompatible-images-20260820.json`

## Removed project clutter

The working project no longer contains rejected candidate datasets, obsolete
candidate checkpoints/evaluations, generated training runs, derived duplicate
source sets, stale build output, duplicate handoff ZIP archives or cache files.
The only candidate dataset retained is the immutable v23 evaluation/reproduction
dataset. The component-detection dataset and its active checkpoint remain because
they are required to retrain the component model.

Removed files were moved recoverably to:

`/Users/Vy/.Trash/SORTRAC-cleanup-20260820/`

They continue to consume disk space until Trash is emptied. Do not empty Trash
until the active app and rollback archive have been independently backed up.

## Verification

- ONNX input/output: `[1, 3, 224, 224]` to `[1, 40]`
- Model label count: 40
- Unit tests: 33 passed
- Production Vite build: passed
- Browser ONNX inference: passed with a real test image
